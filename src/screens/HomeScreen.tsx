import { Button, Heading, VStack } from 'native-base';

const HomeScreen = ({ navigation }) => {
  return (
    <VStack>
      <Heading>This is the home screen!</Heading>
    </VStack>
  );
};

export default HomeScreen;
